using System;
using System.Collections.Generic;
using System.Text;

namespace Store.Services.Helpers
{
    public static class Converter
    {
        public static string ConvertVN(this string originText)
        {
            if (string.IsNullOrEmpty(originText)) return originText;
            const string FindText = "áàảãạâấầẩẫậăắằẳẵặđéèẻẽẹêếềểễệíìỉĩịóòỏõọôốồổỗộơớờởỡợúùủũụưứừửữựýỳỷỹỵÁÀẢÃẠÂẤẦẨẪẬĂẮẰẲẴẶĐÉÈẺẼẸÊẾỀỂỄỆÍÌỈĨỊÓÒỎÕỌÔỐỒỔỖỘƠỚỜỞỠỢÚÙỦŨỤƯỨỪỬỮỰÝỲỶỸỴ";
            const string ReplText = "aaaaaaaaaaaaaaaaadeeeeeeeeeeeiiiiiooooooooooooooooouuuuuuuuuuuyyyyyAAAAAAAAAAAAAAAAADEEEEEEEEEEEIIIIIOOOOOOOOOOOOOOOOOUUUUUUUUUUUYYYYY";
            int index = -1;
            char[] arrChar = FindText.ToCharArray();
            while ((index = originText.IndexOfAny(arrChar)) != -1)
            {
                int index2 = FindText.IndexOf(originText[index]);
                originText = originText.Replace(originText[index], ReplText[index2]);
            }
            return originText;
        }
    }
}
